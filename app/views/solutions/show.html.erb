Parabéns, desafio <%= @solution.started_challenge.challenge.title %>, concluído!

<%= render partial: 'comments/form', locals: { model: @solution} %>
<%= render partial: 'comments/comments', locals: { comments: @comments} %>
